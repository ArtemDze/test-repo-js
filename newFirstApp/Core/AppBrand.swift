import Foundation

/// Canonical product naming — keep UI, a11y, and store-facing copy aligned.
enum AppBrand {
    /// Short brand mark (wordmark / icon line).
    static let name = "Jestora"
    /// Full product name.
    static let fullName = "Jestora Pattern Studio"
    /// Product line under the brand (sentence case).
    static let studioLine = "Pattern Studio"
    /// Uppercased wordmark line.
    static let nameMark = "JESTORA"
    /// Uppercased subtitle under the wordmark.
    static let studioMark = "PATTERN STUDIO"
}
